# Copy these to your static host (Vercel / Netlify / Cloudflare Pages → Environment variables)
# Production scope only — never commit real values.

VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_public_key
VITE_SITE_URL=https://finelycred.com
VITE_SUPABASE_PRIVATE_BUCKET=pii

# Optional — enable when live
# VITE_STRIPE_PUBLISHABLE_KEY=pk_live_...
# VITE_SMARTCREDIT_PID=your_live_pid
# VITE_SENTRY_DSN=https://...

# Edge function secrets go in Supabase dashboard (NOT here):
# SUPABASE_SERVICE_ROLE_KEY, APP_BASE_URL, SENDGRID_API_KEY, TWILIO_AUTH_TOKEN,
# STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, CARTESIA_API_KEY, OPENAI_API_KEY, META_APP_SECRET
