# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.cursor/
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env

# Supabase
supabase/.temp

# Playwright / local QA artifacts
test-results/
playwright-report/
blob-report/
# Roosevelt PII local import (do not commit full credit report)
_import_roosevelt/Roosevelts-Report.html
_import_roosevelt/text-lines.txt
_import_roosevelt/parse-report*.mjs

# Local script tooling (portable ffmpeg ~100MB — not committed)
scripts/.tools/

# Launch demo render cache (intermediate PNG/WAV/segments — not committed)
public/demos/.render-work/

# Committable demo asset (do NOT ignore):
# public/demos/finely-launch-demo.webm

# Windows reserved-name accident (never commit)
NUL.css

# --- Lead / PII CSV exports (never commit real contact lists) ---
# Regenerate legacy audit locally: node scripts/audit-legacy-sql.mjs [path-to-sql]
data/legacy-migration/legacy-partners-audit.csv
data/legacy-migration/legacy-partners-audit-summary.json

# Haitian / FinelyCredit lead lists (owner Documents machine — keep outside repo)
*leads*.csv
*-outreach*.csv
*-outreach-ready*.csv
finely-haitian-leads*.csv
finely-haitian-*.csv
**/Documents/FinelyCredit/**
**/FinelyCredit/**/*.csv

# Warm prospect / marketing desk local seeds
docs/warm-prospects/*.csv
!docs/warm-prospects/*.example.csv

# Allow committed redacted templates only
!*.example.csv
!**/*.example.csv

# Environment backups (never commit secrets)
.env.*
!.env.example
*.env.backup
*.env.bak

# Local dist / deploy zip bundles
*.zip

