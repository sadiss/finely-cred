import React from 'react';
import { PartnerDashboardProductSurface } from './PartnerDashboardProductSurface';

/**
 * Preview alias for the live partner command deck.
 * The old light-surface hub (KPI cards, work modals, leftover launcher) is retired.
 */
export function PartnerDashboardLightSurface() {
  return <PartnerDashboardProductSurface />;
}
